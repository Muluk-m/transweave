---
phase: 10-visual-identity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/tailwind.config.ts
  - packages/web/app/globals.css
autonomous: true
requirements:
  - VIS-04

must_haves:
  truths:
    - "Brand teal and indigo colors are available as Tailwind utility classes (bg-brand-teal, text-brand-indigo, etc.)"
    - "CSS custom properties --brand-teal, --brand-indigo, --brand-gradient-start, --brand-gradient-end exist in :root"
    - "Existing --primary and --accent CSS custom properties are unchanged (Radix UI components still styled correctly)"
    - "Brand tokens are additive — no existing token is modified or removed"
  artifacts:
    - path: "packages/web/tailwind.config.ts"
      provides: "brand color scale in theme.extend.colors"
      contains: "brand:"
    - path: "packages/web/app/globals.css"
      provides: "CSS custom properties for brand colors in :root block"
      contains: "--brand-teal"
  key_links:
    - from: "packages/web/tailwind.config.ts"
      to: "packages/web/app/globals.css"
      via: "brand colors use direct hex values (not hsl(var()) references) — they are static palette values, not semantic tokens"
      pattern: "brand-teal.*#14b8a6"
---

<objective>
Define Transweave brand colors as Tailwind theme extension and CSS custom properties.

Purpose: Landing page (Phase 12), logo components, and OG images need stable brand color references. Adding now as additive tokens ensures Phase 11+ can use brand-teal/brand-indigo without touching semantic tokens.

Output:
- `tailwind.config.ts`: `brand` color scale with teal and indigo variants
- `globals.css`: `--brand-*` CSS custom properties in `:root`
</objective>

<execution_context>
@/Users/qian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-visual-identity/10-RESEARCH.md
@packages/web/tailwind.config.ts
@packages/web/app/globals.css

<interfaces>
<!-- CRITICAL: These existing tokens must NOT change — they drive Radix UI -->
<!-- --primary: 243 75% 59%   (indigo-violet, light mode) -->
<!-- --primary: 243 75% 65%   (indigo-violet, dark mode) -->
<!-- --accent: 199 89% 48%    (cyan, light mode) -->
<!-- --accent: 199 89% 55%    (cyan, dark mode) -->

<!-- Tailwind config existing structure (do not modify these): -->
<!-- theme.extend.colors has: border, input, ring, background, foreground, -->
<!-- primary, secondary, destructive, muted, accent, popover, card, success, warning -->
<!-- ADD brand: {} object alongside these — do not touch existing entries -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add brand color scale to Tailwind config</name>
  <files>packages/web/tailwind.config.ts</files>
  <action>
Open `packages/web/tailwind.config.ts`. Inside `theme.extend.colors`, add a new `brand` key alongside (not replacing) the existing color entries:

```typescript
brand: {
  teal: '#14b8a6',           // teal-500 — primary brand color
  'teal-light': '#f0fdfa',   // teal-50 — for backgrounds, badges, hover tints
  'teal-dark': '#0d9488',    // teal-600 — for favicon background, dark contexts
  'teal-900': '#134e4a',     // teal-900 — for dark mode icon backgrounds
  indigo: '#6366f1',         // indigo-500 — secondary brand color
  'indigo-dark': '#4338ca',  // indigo-700 — for hover states
},
```

Placement: Add this as the last entry inside `theme.extend.colors: { ... }` before the closing brace. Do NOT modify any existing color entries.

This makes classes like `bg-brand-teal`, `text-brand-indigo`, `border-brand-teal-light`, `bg-brand-teal-dark` available throughout the project.

After editing, verify the TypeScript file has no syntax errors by running: `cd packages/web && npx tsc --noEmit 2>&1 | head -20` (expect zero errors related to tailwind.config.ts, though unrelated TS errors may exist).
  </action>
  <verify>
    <automated>grep -A 10 "brand:" /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/web/tailwind.config.ts | grep -q "14b8a6" &amp;&amp; grep -q "6366f1" /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/web/tailwind.config.ts &amp;&amp; grep -q "243 75% 59%" /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/web/tailwind.config.ts &amp;&amp; echo "PASS: brand colors added, primary unchanged"</automated>
  </verify>
  <done>tailwind.config.ts contains brand color scale with teal (#14b8a6) and indigo (#6366f1); existing --primary reference (243 75% 59%) still present and unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Add brand CSS custom properties to globals.css</name>
  <files>packages/web/app/globals.css</files>
  <action>
Open `packages/web/app/globals.css`. Find the `:root` block (light mode variables). Add the following brand custom properties AFTER the last existing variable in the `:root` block, before the closing `}`. Add a comment separating them from the semantic tokens:

```css
  /* Transweave brand identity — static palette (not semantic, not dark-mode adaptive) */
  --brand-teal: 173 80% 40%;           /* #14b8a6 — primary brand teal */
  --brand-teal-50: 166 76% 97%;        /* #f0fdfa — light tint for backgrounds */
  --brand-teal-600: 174 77% 31%;       /* #0d9488 — darker teal for favicon bg */
  --brand-indigo: 239 84% 67%;         /* #6366f1 — secondary brand indigo */
  --brand-indigo-dark: 243 74% 50%;    /* #4338ca — indigo hover state */
  --brand-gradient-start: #14b8a6;     /* teal-500 — gradient start (hex for SVG use) */
  --brand-gradient-end: #6366f1;       /* indigo-500 — gradient end (hex for SVG use) */
```

Do NOT add these inside the `.dark` block — brand colors are static across modes. Do NOT modify any existing variable in `:root` or `.dark`.

Note: `--brand-gradient-start` and `--brand-gradient-end` use hex values intentionally — SVG `stop-color` accepts hex but not `hsl(var(...))` syntax.

After editing, do a quick sanity check: `grep -c "^  --brand" packages/web/app/globals.css` should return 7.
  </action>
  <verify>
    <automated>grep -q "\-\-brand-teal:" /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/web/app/globals.css &amp;&amp; grep -q "\-\-brand-indigo:" /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/web/app/globals.css &amp;&amp; grep -q "\-\-brand-gradient-start" /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/web/app/globals.css &amp;&amp; grep -q "243 75% 59%" /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/web/app/globals.css &amp;&amp; echo "PASS: brand vars added, primary unchanged"</automated>
  </verify>
  <done>globals.css contains all --brand-* custom properties; existing --primary: 243 75% 59% and --accent: 199 89% 48% still present unchanged</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep "brand-teal" packages/web/tailwind.config.ts` — confirms Tailwind brand scale present
2. `grep "brand-indigo" packages/web/tailwind.config.ts` — confirms indigo entry
3. `grep -- "--brand-teal:" packages/web/app/globals.css` — confirms CSS custom property
4. `grep -- "--primary: 243" packages/web/app/globals.css` — confirms primary NOT changed
5. `grep -- "--accent: 199" packages/web/app/globals.css` — confirms accent NOT changed
</verification>

<success_criteria>
Brand color tokens are available in both Tailwind (bg-brand-teal, text-brand-indigo, etc.) and CSS (--brand-teal, --brand-indigo). Existing semantic tokens --primary and --accent are unchanged. No Radix UI component styling is affected.
</success_criteria>

<output>
After completion, create `.planning/phases/10-visual-identity/10-02-SUMMARY.md`
</output>
