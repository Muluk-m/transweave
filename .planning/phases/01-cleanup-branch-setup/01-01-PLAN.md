---
phase: 01-cleanup-branch-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/service/auth.service.ts
  - packages/server/src/controller/auth.controller.ts
  - packages/server/src/service/user.service.ts
  - packages/server/src/models/schemas/user.schema.ts
  - packages/web/app/login/page.tsx
  - packages/web/api/auth.ts
  - packages/web/lib/auth/auth-context.tsx
  - packages/server/src/service/ai.service.ts
  - packages/server/src/utils/superAdmin.ts
  - packages/server/src/modules/database.module.ts
  - packages/server/src/app.module.ts
  - packages/server/src/jwt/strategy.ts
  - packages/web/api/upload.ts
  - packages/web/middleware.ts
  - packages/web/lib/cookies.ts
  - packages/web/components/views/welcomeView.tsx
  - packages/web/components/views/teamsView/teamCard/index.tsx
  - packages/web/components/views/teamsView/teamCard/TeamHeader.tsx
  - packages/web/i18n/en-US.json
  - packages/web/i18n/zh-CN.json
  - packages/web/i18n/all.json
  - packages/web/i18n/messages/en-US.json
  - packages/web/i18n/messages/zh-CN.json
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04
  - CLEAN-05

must_haves:
  truths:
    - "grep -ri 'feishu\\|lark\\|oapi' packages/ returns zero matches"
    - "grep -ri 'qiliangjia\\|bondma\\|maqiqian' packages/ returns zero matches"
    - "grep -r '680f557b' packages/ returns zero matches"
    - "No hardcoded API keys, internal emails, or MongoDB ObjectIds remain in source"
    - "Application code still compiles (tsc --noEmit passes) after removals"
  artifacts:
    - path: "packages/server/src/service/auth.service.ts"
      provides: "Auth service with Feishu code removed"
      contains: "loginWithFeishu method removed or stubbed"
    - path: "packages/server/src/models/schemas/user.schema.ts"
      provides: "User schema without Feishu fields"
      contains: "No feishuId or feishuUnionId fields"
    - path: "packages/server/src/service/ai.service.ts"
      provides: "AI service without hardcoded Dify URL and API key"
      contains: "process.env references only, no hardcoded secrets"
    - path: "packages/server/src/utils/superAdmin.ts"
      provides: "Super admin config without internal emails"
      contains: "Empty array or env-based configuration"
  key_links:
    - from: "packages/server/src/controller/auth.controller.ts"
      to: "packages/server/src/service/auth.service.ts"
      via: "login endpoint still works after Feishu removal"
      pattern: "login.*password"
---

<objective>
Remove all proprietary code, company-specific references, and hardcoded secrets from the codebase.

Purpose: The codebase must be free of any Feishu OAuth code, internal company URLs/domains, hardcoded MongoDB ObjectIds, personal emails, and Dify API keys before the clean orphan branch is created. This is prerequisite work -- if proprietary references remain in source files, they will be committed to the opensource branch.

Output: All source files cleaned of proprietary references while maintaining compilable code.
</objective>

<execution_context>
@/Users/qian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PITFALLS.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove all Feishu OAuth code from backend and frontend</name>
  <files>
    packages/server/src/service/auth.service.ts
    packages/server/src/controller/auth.controller.ts
    packages/server/src/service/user.service.ts
    packages/server/src/models/schemas/user.schema.ts
    packages/web/app/login/page.tsx
    packages/web/api/auth.ts
    packages/web/lib/auth/auth-context.tsx
    packages/web/i18n/zh-CN.json
    packages/web/i18n/en-US.json
    packages/web/i18n/messages/en-US.json
    packages/web/i18n/messages/zh-CN.json
  </files>
  <action>
    Remove all Feishu OAuth integration code. This is a DELETION task -- remove code, do not replace with new auth yet (that is Phase 3). Leave local username/password auth intact.

    **Backend changes (packages/server/):**

    1. `src/service/auth.service.ts`:
       - DELETE the `loginWithFeishu()` method entirely (the one that calls `https://open.feishu.cn/open-apis/authen/v2/oauth/token`)
       - DELETE the `getFeishuAccessToken()` private helper method
       - DELETE the `getFeishuUserInfo()` private helper method
       - DELETE `private readonly clientId = process.env.FEISHU_CLIENT_ID;` and `clientSecret` properties
       - DELETE the `joinDefaultTeam()` method that uses hardcoded ObjectId `'680f557b932fa3656cbae929'`
       - KEEP the `login()` method (local password auth) and `register()` method and `createToken()` method intact
       - KEEP HttpService import if still used by remaining methods; remove if only Feishu used it

    2. `src/controller/auth.controller.ts`:
       - DELETE the `@Post('login_feishu')` endpoint handler
       - KEEP `@Post('login')` and `@Post('register')` endpoints

    3. `src/service/user.service.ts`:
       - DELETE the `findUserByFeishuId()` method
       - In `createUser()`, remove `feishuId` and `feishuUnionId` fields from the object passed to `this.userModel.create()`
       - KEEP all other user CRUD methods

    4. `src/models/schemas/user.schema.ts`:
       - DELETE the `feishuId` property and its `@Prop()` decorator
       - DELETE the `feishuUnionId` property and its `@Prop()` decorator
       - Change the `loginProvider` field: remove `'feishu'` from the enum, keep only `'local'`. Change type to just `'local'` or remove the field entirely since all users will be local.

    **Frontend changes (packages/web/):**

    5. `app/login/page.tsx`:
       - DELETE the `feishuState` constant (`'qiliangjia-i18n'`)
       - DELETE any `FEISHU_CLIENT_ID` or `FEISHU_APP_ID` references
       - DELETE the Feishu redirect URL construction and redirect logic
       - DELETE the `handleFeishuCallback()` function (the one that calls `loginWithFeishu` API)
       - DELETE any useEffect that checks for `code` query parameter for OAuth callback
       - DELETE the "Login with Feishu" button and its associated UI
       - KEEP the local login form (username/password)

    6. `api/auth.ts`:
       - DELETE the `loginWithFeishu()` API function (the one that POSTs to `/login_feishu`)
       - KEEP `login()`, `register()`, `checkAuthStatus()` functions

    7. `lib/auth/auth-context.tsx`:
       - DELETE any Feishu-specific auth state or methods if present
       - KEEP the JWT-based auth context (token storage, refresh, logout)

    8. i18n files (`i18n/zh-CN.json`, `i18n/en-US.json`, `i18n/messages/en-US.json`, `i18n/messages/zh-CN.json`):
       - Search for and remove any Feishu-specific UI text strings (e.g., "Login with Feishu", "Feishu login" etc.)
       - KEEP all other translation strings

    **Important:** After all deletions, verify that remaining code has no broken imports or references to deleted functions. The app should still compile with `tsc --noEmit` (there will be other issues from later cleanup, but no Feishu-related type errors).
  </action>
  <verify>
    <automated>cd /Users/qian/Projects/Qlj/qlj-fe-i18n && grep -ri 'feishu\|lark\|oapi' packages/ --include='*.ts' --include='*.tsx' --include='*.json' | grep -v node_modules | grep -v '.planning' | wc -l | xargs test 0 -eq && echo "PASS: zero feishu matches" || echo "FAIL: feishu references remain"</automated>
  </verify>
  <done>
    - `grep -ri 'feishu\|lark\|oapi' packages/` returns zero matches (excluding node_modules)
    - `grep -r '680f557b' packages/` returns zero matches
    - No `loginWithFeishu`, `findUserByFeishuId`, or `login_feishu` endpoint exist in codebase
    - Local login/register endpoints and functions remain intact
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove internal company references, branding, hardcoded secrets, and business-specific config</name>
  <files>
    packages/server/src/service/ai.service.ts
    packages/server/src/utils/superAdmin.ts
    packages/server/src/modules/database.module.ts
    packages/server/src/app.module.ts
    packages/server/src/jwt/strategy.ts
    packages/web/api/upload.ts
    packages/web/middleware.ts
    packages/web/lib/cookies.ts
    packages/web/components/views/welcomeView.tsx
    packages/web/components/views/teamsView/teamCard/index.tsx
    packages/web/components/views/teamsView/teamCard/TeamHeader.tsx
    packages/web/i18n/en-US.json
    packages/web/i18n/zh-CN.json
    packages/web/i18n/all.json
    packages/web/i18n/messages/en-US.json
    packages/web/i18n/messages/zh-CN.json
  </files>
  <action>
    Remove all internal company references, hardcoded secrets, and business-specific configuration. Replace with environment variable references or generic placeholders.

    **Hardcoded secrets and API keys:**

    1. `packages/server/src/service/ai.service.ts`:
       - Replace `const DIFY_API_URL = 'https://api-ai.qiliangjia.org/v1/chat-messages';` with `const DIFY_API_URL = process.env.DIFY_API_URL || '';`
       - Replace `const DIFY_API_KEY = process.env.DIFY_API_KEY || 'app-QFt9YXsFIT9YMeVcvn9muuaR';` with `const DIFY_API_KEY = process.env.DIFY_API_KEY || '';`
       - The hardcoded API key `app-QFt9YXsFIT9YMeVcvn9muuaR` is a real Dify key that MUST be removed

    2. `packages/server/src/app.module.ts`:
       - Change `secret: process.env.JWT_SECRET || 'your-secret-key'` to `secret: process.env.JWT_SECRET` (remove the fallback -- server should not start without JWT_SECRET set; this is a security issue noted in PITFALLS.md)

    3. `packages/server/src/jwt/strategy.ts`:
       - Same change: `secretOrKey: process.env.JWT_SECRET || 'your-secret-key'` to `secretOrKey: process.env.JWT_SECRET` (remove the insecure fallback)

    **Internal company references:**

    4. `packages/server/src/utils/superAdmin.ts`:
       - Replace `export const SUPER_ADMINS = ['maqiqian@qiliangjia.com']` with `export const SUPER_ADMINS: string[] = []` (empty array; admin will be configured via first-run setup in Phase 3)

    5. `packages/server/src/modules/database.module.ts`:
       - Replace the fallback MongoDB connection string `'mongodb://admin:secret@localhost:27017/bondma?authSource=admin&replicaSet=rs0'` with `''` or remove the fallback entirely. The DATABASE_URL env var will be required.

    6. `packages/web/api/upload.ts`:
       - Replace both hardcoded CDN URLs `'https://qlj-devhub-homepage.qiliangjia.one/api/uploads'` with a reference to the API base URL: use `${API_BASE}/uploads` or the pattern used in other API files (check how `API_BASE` is constructed in the file or in `lib/api.ts`)

    7. `packages/web/middleware.ts`:
       - Remove or comment out any reference to `qlj-fe-i18n.qiliangjia.org` (there is a commented-out line referencing this URL)

    **Company branding (Bondma):**

    8. `packages/web/lib/cookies.ts`:
       - Rename `bondma_language` cookie key to `i18n_language` (or similar generic name)

    9. `packages/web/components/views/welcomeView.tsx`:
       - Replace `alt="Bondma ..."` with generic alt text like `alt="Platform preview"`

    10. `packages/web/components/views/teamsView/teamCard/index.tsx` and `TeamHeader.tsx`:
        - Replace `bondma.com/team/` with a generic placeholder like `i18n-platform/team/` or make it configurable. These appear to be display-only URL prefixes.

    11. i18n JSON files (`en-US.json`, `zh-CN.json`, `all.json`, `messages/en-US.json`, `messages/zh-CN.json`):
        - Replace ALL occurrences of "Bondma" and "BondMa" with a generic placeholder "i18n Platform" (English) and "i18n 平台" (Chinese)
        - Replace `bondma.com/team/` URL prefix with `app/team/`
        - These are marketing/branding strings that must not reference the internal product name
        - Be thorough -- the grep found 40+ occurrences across these files

    **Other cleanup:**

    12. `init/init-replica.js` -- this file is a MongoDB replica set initialization script. Leave it for now (it will be removed when MongoDB is fully replaced in Phase 2), but note it contains `bondma` as database name.

    **After all changes**, do a final comprehensive grep to verify:
    - `grep -ri 'qiliangjia' packages/` returns zero matches
    - `grep -ri 'bondma' packages/` returns zero matches (excluding node_modules)
    - `grep -ri 'maqiqian' packages/` returns zero matches
    - `grep -r 'app-QFt9YXsFIT9YMeVcvn9muuaR' packages/` returns zero matches
    - `grep -r "your-secret-key" packages/` returns zero matches
  </action>
  <verify>
    <automated>cd /Users/qian/Projects/Qlj/qlj-fe-i18n && MATCHES=$(grep -ri 'qiliangjia\|bondma\|maqiqian\|app-QFt9YXsFIT9YMeVcvn9muuaR\|your-secret-key' packages/ --include='*.ts' --include='*.tsx' --include='*.json' --include='*.js' | grep -v node_modules | grep -v '.planning' | wc -l | tr -d ' ') && test "$MATCHES" -eq 0 && echo "PASS: zero proprietary matches" || echo "FAIL: $MATCHES proprietary references remain"</automated>
  </verify>
  <done>
    - `grep -ri 'qiliangjia\|bondma\|maqiqian' packages/` returns zero matches (excluding node_modules)
    - No hardcoded Dify API key in source
    - No fallback JWT secret in source
    - No internal emails in source
    - No hardcoded MongoDB connection string with company database name
    - All CDN URLs replaced with API base URL references
    - All i18n branding strings use generic "i18n Platform" / "i18n 平台" instead of "Bondma"
  </done>
</task>

</tasks>

<verification>
Run both verification commands sequentially:

```bash
# 1. Zero Feishu/OAuth references
grep -ri 'feishu\|lark\|oapi' packages/ --include='*.ts' --include='*.tsx' --include='*.json' | grep -v node_modules | wc -l
# Expected: 0

# 2. Zero internal company references
grep -ri 'qiliangjia\|bondma\|maqiqian\|app-QFt9YXsFIT9YMeVcvn9muuaR\|your-secret-key' packages/ --include='*.ts' --include='*.tsx' --include='*.json' | grep -v node_modules | wc -l
# Expected: 0

# 3. Zero hardcoded MongoDB ObjectIds
grep -r '680f557b' packages/ --include='*.ts' | grep -v node_modules | wc -l
# Expected: 0

# 4. TypeScript still compiles (server)
cd packages/server && npx tsc --noEmit 2>&1 | tail -5
```
</verification>

<success_criteria>
1. Zero matches for feishu, lark, oapi across all packages
2. Zero matches for qiliangjia, bondma, maqiqian across all packages
3. Zero hardcoded API keys, secrets, or MongoDB ObjectIds in source code
4. Local auth endpoints (login, register) remain functional
5. All i18n branding replaced with generic names
</success_criteria>

<output>
After completion, create `.planning/phases/01-cleanup-branch-setup/01-01-SUMMARY.md`
</output>
