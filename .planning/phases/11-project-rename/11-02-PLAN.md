---
phase: 11-project-rename
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - packages/cli/bin/qlj-i18n.js
  - packages/cli/bin/transweave.js
  - packages/cli/src/config.ts
  - packages/cli/src/index.ts
  - packages/cli/src/commands/init.ts
  - packages/cli/src/commands/pull.ts
  - packages/cli/src/commands/push.ts
autonomous: true
requirements:
  - REN-03

must_haves:
  truths:
    - "CLI binary is named transweave (bin/transweave.js exists, bin/qlj-i18n.js is gone)"
    - "CLI config directory is ~/.config/transweave/ and project config file is .transweave.json"
    - "CLI environment variables are TRANSWEAVE_API_KEY and TRANSWEAVE_SERVER"
    - "All user-facing CLI messages say transweave instead of qlj-i18n"
    - "qlji_ references in login.ts are untouched (they are the API key prefix, not CLI name)"
  artifacts:
    - path: "packages/cli/bin/transweave.js"
      provides: "CLI entry point with new binary name"
    - path: "packages/cli/src/config.ts"
      provides: "Config paths and env vars using Transweave naming"
      contains: "TRANSWEAVE_API_KEY"
    - path: "packages/cli/src/index.ts"
      provides: "CLI program named transweave"
      contains: ".name('transweave')"
  key_links:
    - from: "packages/cli/package.json"
      to: "packages/cli/bin/transweave.js"
      via: "bin field mapping"
      pattern: '"transweave": "./bin/transweave.js"'
---

<objective>
Rename the CLI binary file, update all CLI source files to use Transweave naming for the binary name, config directory, config filename, and environment variable names. Preserve all `qlji_` references in login.ts unchanged.

Purpose: The CLI is the primary developer touchpoint. Users will run `transweave pull` and `transweave push`. Config must live at `~/.config/transweave/` and env vars must be `TRANSWEAVE_API_KEY`/`TRANSWEAVE_SERVER`.
Output: CLI binary `bin/transweave.js`, updated source in 5 files. Depends on Plan 01 (package.json bin field already updated to reference `transweave.js`).
</objective>

<execution_context>
@/Users/qian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-project-rename/11-RESEARCH.md
@.planning/phases/11-project-rename/11-01-SUMMARY.md

**CRITICAL: DO NOT touch `qlji_` in login.ts.** Lines 8, 12, 13 of `packages/cli/src/commands/login.ts` reference `qlji_` — this is the API key prefix that users must type. It is NOT the CLI tool name. Leave it unchanged.

**bin file rename:** Use `git mv` to rename `packages/cli/bin/qlj-i18n.js` to `packages/cli/bin/transweave.js` so git history is preserved. The file content stays identical (`#!/usr/bin/env node\nrequire('../dist/index.js');`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename CLI bin file using git mv</name>
  <files>packages/cli/bin/qlj-i18n.js, packages/cli/bin/transweave.js</files>
  <action>
Run: `git mv /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/cli/bin/qlj-i18n.js /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/cli/bin/transweave.js`

This renames the file and stages the rename in git history. The file content does NOT change — it remains:
```
#!/usr/bin/env node
require('../dist/index.js');
```

After the rename, verify the new file exists and the old file is gone.
  </action>
  <verify>
    <automated>ls /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/cli/bin/ && git -C /Users/qian/Projects/Qlj/qlj-fe-i18n status --short packages/cli/bin/</automated>
  </verify>
  <done>
    `packages/cli/bin/transweave.js` exists.
    `packages/cli/bin/qlj-i18n.js` does not exist.
    `git status` shows the rename (R qlj-i18n.js -> transweave.js).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CLI config.ts — paths and env vars</name>
  <files>packages/cli/src/config.ts</files>
  <action>
Edit `packages/cli/src/config.ts` with these changes (use exact line numbers from research):

- Line 17: `'.config', 'qlj-i18n'` → `'.config', 'transweave'`
- Line 19: `'.qlj-i18n.json'` → `'.transweave.json'`
- Line 22 (comment): `~/.config/qlj-i18n/config.json` → `~/.config/transweave/config.json`
- Line 37 (comment): `~/.config/qlj-i18n/config.json` → `~/.config/transweave/config.json`
- Line 45 (comment): `.qlj-i18n.json` → `.transweave.json`
- Line 61 (comment): `.qlj-i18n.json` → `.transweave.json`
- Line 70 (comment): `QLJ_I18N_API_KEY` → `TRANSWEAVE_API_KEY`
- Line 73: `process.env.QLJ_I18N_API_KEY` → `process.env.TRANSWEAVE_API_KEY`
- Line 83 (comment): `QLJ_I18N_SERVER` → `TRANSWEAVE_SERVER`
- Line 86: `process.env.QLJ_I18N_SERVER` → `process.env.TRANSWEAVE_SERVER`

Do NOT touch any other lines. This file contains no `qlji_` references — the rename targets here are the CLI tool's own config paths and env vars, not the API key prefix.
  </action>
  <verify>
    <automated>grep -n "qlj-i18n\|QLJ_I18N\|transweave\|TRANSWEAVE" /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/cli/src/config.ts</automated>
  </verify>
  <done>
    Zero `qlj-i18n` or `QLJ_I18N` strings remain in config.ts.
    `TRANSWEAVE_API_KEY` and `TRANSWEAVE_SERVER` appear as env var names.
    Config dir is `.config/transweave` and project config file is `.transweave.json`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update CLI index.ts and command files</name>
  <files>packages/cli/src/index.ts, packages/cli/src/commands/init.ts, packages/cli/src/commands/pull.ts, packages/cli/src/commands/push.ts</files>
  <action>
Edit `packages/cli/src/index.ts`:
- Line 8: `.name('qlj-i18n')` → `.name('transweave')`
- Line 9: `'CLI for qlj-i18n translation management'` → `'CLI for Transweave translation management'`

Edit `packages/cli/src/commands/init.ts`:
- Line 6 (description): `(.qlj-i18n.json)` → `(.transweave.json)`
- Line 16: `Run "qlj-i18n login" first.` → `Run "transweave login" first."`
- Line 44: `Initialized qlj-i18n config for project` → `Initialized Transweave config for project`
- Line 49: `Config saved to .qlj-i18n.json` → `Config saved to .transweave.json`

Edit `packages/cli/src/commands/pull.ts`:
- Line 22: `Run "qlj-i18n init" first.` → `Run "transweave init" first."`
- Line 29: `Run "qlj-i18n login" first.` → `Run "transweave login" first."`

Edit `packages/cli/src/commands/push.ts`:
- Line 17: `Run "qlj-i18n init" first.` → `Run "transweave init" first."`
- Line 24: `Run "qlj-i18n login" first.` → `Run "transweave login" first."`

**Do NOT touch `packages/cli/src/commands/login.ts`** — that file contains `qlji_` API key prefix references (lines 8, 12, 13) that must stay exactly as they are.
  </action>
  <verify>
    <automated>grep -rn "qlj-i18n" /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/cli/src/index.ts /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/cli/src/commands/init.ts /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/cli/src/commands/pull.ts /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/cli/src/commands/push.ts 2>/dev/null; echo "---preserved qlji_ in login.ts:---"; grep -n "qlji_" /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/cli/src/commands/login.ts</automated>
  </verify>
  <done>
    Zero `qlj-i18n` strings in index.ts, init.ts, pull.ts, push.ts.
    `login.ts` still has `qlji_` references on lines 8, 12, 13 — unchanged.
    CLI program name is `transweave`, description says "Transweave".
  </done>
</task>

</tasks>

<verification>
After all three tasks, run a complete CLI directory check:

```bash
# Confirm bin file is renamed
ls /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/cli/bin/

# Confirm zero old-brand strings in all CLI source files (login.ts excluded from rename targets)
grep -rn "qlj-i18n" \
  /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/cli/src/ \
  /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/cli/bin/

# Confirm qlji_ is preserved in login.ts
grep -n "qlji_" /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/cli/src/commands/login.ts
```
Expected: first grep returns zero lines; last grep shows qlji_ references still present.
</verification>

<success_criteria>
- `packages/cli/bin/transweave.js` exists; `packages/cli/bin/qlj-i18n.js` does not
- CLI config.ts: config dir is `transweave`, project config is `.transweave.json`, env vars are `TRANSWEAVE_API_KEY` and `TRANSWEAVE_SERVER`
- CLI program named `transweave` in index.ts
- All error messages in init.ts, pull.ts, push.ts say `transweave` instead of `qlj-i18n`
- `login.ts` is untouched — `qlji_` API key prefix preserved
</success_criteria>

<output>
After completion, create `.planning/phases/11-project-rename/11-02-SUMMARY.md` following the summary template.
</output>
