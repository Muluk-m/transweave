---
phase: 11-project-rename
plan: 05
type: execute
wave: 3
depends_on:
  - 11-02
  - 11-03
  - 11-04
files_modified:
  - README.md
  - docs/api-reference.md
  - .env.example
  - .env
autonomous: true
requirements:
  - REN-07
  - REN-08

must_haves:
  truths:
    - "README.md title and all CLI examples use transweave instead of qlj-i18n"
    - "docs/api-reference.md title, MCP config key, npm install command, CLI command table, and env var table use Transweave naming"
    - ".env.example header comment says Transweave Environment Configuration"
    - ".env header comment says Transweave Environment Configuration"
    - "qlji_ API key prefix is preserved everywhere in documentation (it is the actual prefix users must type)"
    - "qlj-i18n-ai-salt encryption salt documentation is preserved unchanged"
  artifacts:
    - path: "README.md"
      provides: "Branded README with Transweave naming"
      contains: "transweave"
    - path: "docs/api-reference.md"
      provides: "API reference with Transweave naming"
      contains: "transweave-mcp-server"
    - path: ".env.example"
      provides: "Environment variable template with Transweave header"
      contains: "Transweave Environment"
  key_links:
    - from: "docs/api-reference.md"
      to: "CLI and MCP documentation"
      via: "command table and config examples"
      pattern: "transweave pull|transweave push"
---

<objective>
Update all documentation files to use Transweave naming: README.md title and CLI command examples, api-reference.md title and all command references, and .env.example/.env header comments. Preserve all `qlji_` API key prefix references and `qlj-i18n-ai-salt` salt references — these are documentation of actual infrastructure values users must use.

Purpose: Documentation is what developers see on GitHub and when setting up the tool. Stale branding here undermines the rename.
Output: 4 files updated. Wave 3 — executes after Plans 02, 03, 04 complete.
</objective>

<execution_context>
@/Users/qian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-project-rename/11-RESEARCH.md
@.planning/phases/11-project-rename/11-02-SUMMARY.md
@.planning/phases/11-project-rename/11-03-SUMMARY.md

**CRITICAL: Preserve these exactly:**
- `qlji_` API key prefix everywhere in docs — this is the actual prefix users must type. Every `Bearer qlji_...` example must stay.
- `qlj-i18n-ai-salt` if it appears in any documentation — it is the AES-256-GCM salt.

**Never do a global "qlj" replace** — only replace `qlj-i18n` (hyphenated) patterns. Bare `qlj` must not be changed.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md</name>
  <files>README.md</files>
  <action>
Read the full README.md first to understand its current content, then make these targeted changes:

1. Line 1 (title): If it reads `# qlj-i18n` or `# qlj-i18n Translation Manager`, change to `# Transweave`
2. All CLI command examples: Replace `qlj-i18n pull` → `transweave pull`, `qlj-i18n push` → `transweave push`, `qlj-i18n login` → `transweave login`, `qlj-i18n init` → `transweave init`
3. Any repository URL references like `qlj-fe-i18n` in GitHub URLs: Leave unchanged (the repository name itself is not being renamed in this phase — only the project brand name)
4. Directory listing lines showing `qlj-i18n/` in directory names: Update to `transweave/`
5. Any `qlj-i18n-server` mentions: Change to `@transweave/server`

**Preserve unchanged:**
- `qlji_` API key prefix examples (e.g., `qlji_your_key_here` in API key setup instructions)
- Repository URL `qlj-fe-i18n` — repo name is unchanged
  </action>
  <verify>
    <automated>grep -n "qlj-i18n\|@qlj/" /Users/qian/Projects/Qlj/qlj-fe-i18n/README.md | grep -v "qlji_" | head -20</automated>
  </verify>
  <done>
    Zero `qlj-i18n` strings remain in README.md (except any `qlji_` API key prefix examples, which are excluded from rename).
    All CLI commands say `transweave`, not `qlj-i18n`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update docs/api-reference.md</name>
  <files>docs/api-reference.md</files>
  <action>
Read the full api-reference.md first, then make these targeted changes based on the research inventory:

1. Line 1 (title): Change from old title to `# Transweave API Reference`
2. Line 1197 (MCP config key): Change `"qlj-i18n": {` to `"transweave": {`
3. Line 1244 (npm install command): Change `npm install -g qlj-i18n` to `npm install -g transweave`
4. CLI command table rows (lines ~1251-1254): Change all `qlj-i18n pull`, `qlj-i18n push`, etc. to `transweave pull`, `transweave push`, etc.
5. Env var table (lines ~1260-1261): Change `QLJ_I18N_API_KEY` → `TRANSWEAVE_API_KEY`, `QLJ_I18N_SERVER` → `TRANSWEAVE_SERVER`

**Preserve unchanged (these are actual user-facing values, not branding):**
- All `qlji_` API key prefix references (e.g., `Bearer qlji_YOUR_API_KEY_HERE` in auth examples — these document the real prefix)
- The `qlj-i18n-ai-salt` value if it appears (encryption salt, not a brand name)

Use the exact line numbers from the research as a guide, but read the file first to confirm current line numbers before editing (they may have shifted slightly).
  </action>
  <verify>
    <automated>grep -n "qlj-i18n\|QLJ_I18N\|@qlj/" /Users/qian/Projects/Qlj/qlj-fe-i18n/docs/api-reference.md | grep -v "qlji_" | head -20</automated>
  </verify>
  <done>
    Zero `qlj-i18n` (hyphenated) or `QLJ_I18N` strings remain in api-reference.md, except `qlji_` API key prefix examples which are preserved.
    MCP config key is `"transweave"`, npm install is `npm install -g transweave`, CLI commands say `transweave`.
    Env vars say `TRANSWEAVE_API_KEY` and `TRANSWEAVE_SERVER`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update .env.example and .env header comments</name>
  <files>.env.example, .env</files>
  <action>
Edit `.env.example`:
- Line 2: Change `# qlj-i18n Environment Configuration` to `# Transweave Environment Configuration`

Edit `.env`:
- Line 2: Change `# qlj-i18n Environment Configuration` to `# Transweave Environment Configuration`

Note: `.env` is gitignored but exists locally. Update it so the developer sees consistent branding in their local env file. This is a comment-only change in both files.

Do NOT change any actual environment variable names or values in these files. The rename of CLI env vars (`QLJ_I18N_API_KEY` → `TRANSWEAVE_API_KEY`) was done in Plan 02; these files only need the header comment updated.
  </action>
  <verify>
    <automated>head -3 /Users/qian/Projects/Qlj/qlj-fe-i18n/.env.example && echo "---" && head -3 /Users/qian/Projects/Qlj/qlj-fe-i18n/.env 2>/dev/null</automated>
  </verify>
  <done>
    `.env.example` line 2 reads `# Transweave Environment Configuration`.
    `.env` line 2 reads `# Transweave Environment Configuration`.
  </done>
</task>

</tasks>

<verification>
After all three tasks, run a broad check across all documentation:

```bash
# Check for remaining old-brand strings in docs (qlji_ should NOT be flagged)
grep -rn "qlj-i18n\|@qlj/\|QLJ_I18N" \
  /Users/qian/Projects/Qlj/qlj-fe-i18n/README.md \
  /Users/qian/Projects/Qlj/qlj-fe-i18n/docs/ \
  /Users/qian/Projects/Qlj/qlj-fe-i18n/.env.example \
  2>/dev/null | grep -v "qlji_"

# Confirm qlji_ preserved in docs
grep -c "qlji_" /Users/qian/Projects/Qlj/qlj-fe-i18n/docs/api-reference.md
```
Expected: first command returns zero lines; second command returns count > 0 (qlji_ still documented).
</verification>

<success_criteria>
- README.md title is Transweave; all CLI command examples use `transweave`
- api-reference.md: title is Transweave, MCP key is `"transweave"`, npm command is `npm install -g transweave`, CLI command table uses `transweave`, env var table uses `TRANSWEAVE_API_KEY`/`TRANSWEAVE_SERVER`
- `.env.example` header is `# Transweave Environment Configuration`
- `.env` header is `# Transweave Environment Configuration`
- All `qlji_` API key prefix examples are preserved in documentation
</success_criteria>

<output>
After completion, create `.planning/phases/11-project-rename/11-05-SUMMARY.md` following the summary template.
</output>
