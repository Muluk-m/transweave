---
phase: 11-project-rename
plan: 06
type: execute
wave: 4
depends_on:
  - 11-05
files_modified: []
autonomous: false
requirements:
  - REN-08
  - REN-09

must_haves:
  truths:
    - "grep for qlj-i18n, @qlj/, QLJ_I18N, and nextjs returns zero matches across all source files"
    - "grep for qlji_ and qlj-i18n-ai-salt returns matches (proving they were preserved)"
    - "User has verified the preserved identifiers are exactly as expected"
  artifacts:
    - path: "packages/server/src/ai/encryption.util.ts"
      provides: "Preserved qlj-i18n-ai-salt encryption salt"
      contains: "qlj-i18n-ai-salt"
    - path: "packages/server/src/jwt/guard.ts"
      provides: "Preserved qlji_ API key routing signal"
      contains: "qlji_"
  key_links:
    - from: "verification grep"
      to: "all source files"
      via: "zero-match confirmation"
      pattern: "qlj-i18n|@qlj/|QLJ_I18N|\"nextjs\""
---

<objective>
Run the verification grep to confirm zero remaining old-brand references across all source files, and confirm the two load-bearing preserved identifiers (`qlji_` and `qlj-i18n-ai-salt`) are still intact. Human checkpoint to review the grep output.

Purpose: The rename is a pure text-substitution task — systematic verification is the only way to guarantee completeness. This is the final gate before Phase 11 is considered done.
Output: Confirmed clean codebase with preserved infrastructure identifiers. No files modified.
</objective>

<execution_context>
@/Users/qian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-project-rename/11-RESEARCH.md
@.planning/phases/11-project-rename/11-05-SUMMARY.md

**Preserved identifiers (MUST still exist after rename):**
- `qlji_` in `packages/server/src/service/api-key.service.ts:22`, `guard.ts:37`, `mcp.controller.ts:28,33`, `cli/src/commands/login.ts:12,13`
- `qlj-i18n-ai-salt` in `packages/server/src/ai/encryption.util.ts:17`

**Verification approach:** Run the grep commands from the research document. The "clean" grep must return zero matches. The "preserved" grep must return matches.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run rename completeness verification grep</name>
  <files></files>
  <action>
Run the following two verification commands and capture their output:

**Command 1 — Should return ZERO matches:**
```bash
grep -rn "qlj-i18n\|@qlj/\|QLJ_I18N\|\"nextjs\"" \
  --include="*.ts" --include="*.tsx" --include="*.json" \
  --include="*.yml" --include="*.yaml" --include="*.md" \
  --include="*.js" --include="*.env*" \
  --exclude-dir=node_modules --exclude-dir=.next --exclude-dir=dist \
  /Users/qian/Projects/Qlj/qlj-fe-i18n
```

**Command 2 — Should return MATCHES (preserved identifiers):**
```bash
grep -rn "qlji_\|qlj-i18n-ai-salt" \
  --include="*.ts" \
  --exclude-dir=node_modules \
  /Users/qian/Projects/Qlj/qlj-fe-i18n
```

If Command 1 returns any matches, list the file paths and lines. These are missed renames that need to be fixed before proceeding. Address any found issues immediately in this task by making the targeted edit (treating it as a missed rename from earlier plans).

If Command 2 returns zero matches, that is a CRITICAL failure — the load-bearing identifiers have been accidentally renamed. Stop and report immediately.
  </action>
  <verify>
    <automated>grep -rn "qlj-i18n\|@qlj/\|QLJ_I18N\|\"nextjs\"" --include="*.ts" --include="*.tsx" --include="*.json" --include="*.yml" --include="*.yaml" --include="*.md" --include="*.js" --include="*.env*" --exclude-dir=node_modules --exclude-dir=.next --exclude-dir=dist /Users/qian/Projects/Qlj/qlj-fe-i18n 2>/dev/null | wc -l</automated>
  </verify>
  <done>
    Command 1 returns zero matches (count = 0).
    Command 2 returns matches for `qlji_` (in guard.ts, api-key.service.ts, login.ts, mcp.controller.ts) and `qlj-i18n-ai-salt` (in encryption.util.ts).
    Any stray matches found by Command 1 have been fixed in this task.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 2: Final review and approval of Phase 11 rename</name>
  <files></files>
  <action>
PAUSE — human review of verification results required.

The automated grep verification has run. Review the output from Task 1:
- Command 1 (old-brand grep) should show ZERO matches
- Command 2 (preserved-identifiers grep) should show matches in encryption.util.ts, guard.ts, api-key.service.ts, mcp.controller.ts, and login.ts

Spot-check these key indicators:
1. `packages/cli/bin/transweave.js` exists
2. `packages/cli/bin/qlj-i18n.js` does NOT exist
3. `packages/server/src/ai/encryption.util.ts` line 17 — still says `qlj-i18n-ai-salt`
4. `packages/server/src/jwt/guard.ts` — still says `qlji_`
  </action>
  <verify>
    <automated>ls /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/cli/bin/ && grep -c "qlji_" /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/server/src/ai/encryption.util.ts 2>/dev/null || echo "0"</automated>
  </verify>
  <done>User has reviewed verification results and typed "approved". Phase 11 rename is complete.</done>
  <what-built>
    All rename work is complete. The verification grep has been run:

    1. Old-brand grep (qlj-i18n, @qlj/, QLJ_I18N, "nextjs") — should show ZERO matches
    2. Preserved-identifiers grep (qlji_, qlj-i18n-ai-salt) — should show matches in:
       - `encryption.util.ts` (qlj-i18n-ai-salt salt)
       - `guard.ts` (qlji_ routing signal)
       - `api-key.service.ts` (qlji_ prefix)
       - `mcp.controller.ts` (qlji_ in auth header docs)
       - `login.ts` (qlji_ prefix validation message)
  </what-built>
  <how-to-verify>
    1. Review the grep output from Task 1. Confirm zero old-brand matches.
    2. Confirm the preserved identifiers are present in the expected files.
    3. Optionally spot-check a few key files:
       - `packages/cli/bin/transweave.js` — exists
       - `packages/cli/bin/qlj-i18n.js` — does NOT exist
       - `packages/server/src/ai/encryption.util.ts` line 17 — still says `qlj-i18n-ai-salt`
       - `packages/server/src/jwt/guard.ts` — still says `qlji_`
    4. Optionally run: `pnpm install` to confirm workspace resolves with new package names (no filter errors)
  </how-to-verify>
  <resume-signal>Type "approved" if verification passes, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Final phase verification commands:

```bash
# Zero old-brand references
grep -rn "qlj-i18n\|@qlj/\|QLJ_I18N\|\"nextjs\"" \
  --include="*.ts" --include="*.tsx" --include="*.json" \
  --include="*.yml" --include="*.yaml" --include="*.md" \
  --include="*.js" --include="*.env*" \
  --exclude-dir=node_modules --exclude-dir=.next --exclude-dir=dist \
  /Users/qian/Projects/Qlj/qlj-fe-i18n

# Preserved infrastructure identifiers still present
grep -rn "qlji_\|qlj-i18n-ai-salt" \
  --include="*.ts" \
  --exclude-dir=node_modules \
  /Users/qian/Projects/Qlj/qlj-fe-i18n

# CLI bin file exists with new name
ls /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/cli/bin/
```
</verification>

<success_criteria>
- Zero matches for `qlj-i18n`, `@qlj/`, `QLJ_I18N`, `"nextjs"` across all source files
- `qlji_` matches found in guard.ts, api-key.service.ts, mcp.controller.ts, login.ts
- `qlj-i18n-ai-salt` match found in encryption.util.ts
- `packages/cli/bin/transweave.js` exists; `packages/cli/bin/qlj-i18n.js` does not
- User has approved verification via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/11-project-rename/11-06-SUMMARY.md` following the summary template.
</output>
