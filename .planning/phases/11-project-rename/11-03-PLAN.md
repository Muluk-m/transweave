---
phase: 11-project-rename
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - packages/server/src/service/mcp.service.ts
  - packages/server/src/controller/mcp.controller.ts
  - packages/web/i18n/all.json
autonomous: true
requirements:
  - REN-04
  - REN-05

must_haves:
  truths:
    - "MCP server registers as transweave-mcp-server"
    - "MCP HTML documentation shows transweave config key instead of qlj-i18n"
    - "Web UI header title shows Transweave in both en-US and zh-CN"
    - "qlji_ API key prefix in mcp.controller.ts documentation is untouched"
  artifacts:
    - path: "packages/server/src/service/mcp.service.ts"
      provides: "MCP server name registration"
      contains: "transweave-mcp-server"
    - path: "packages/server/src/controller/mcp.controller.ts"
      provides: "MCP HTML docs with updated config key"
      contains: '"transweave"'
    - path: "packages/web/i18n/all.json"
      provides: "Web UI header title string"
      contains: '"Transweave"'
  key_links:
    - from: "packages/server/src/service/mcp.service.ts"
      to: "McpServer constructor"
      via: "name property"
      pattern: "name: 'transweave-mcp-server'"
---

<objective>
Update the MCP server name from `qlj-i18n-mcp-server` to `transweave-mcp-server` in two server files, and update the single remaining web UI i18n string.

Purpose: The MCP server name is what AI coding assistants see when they connect. The web header title is visible to every user who opens the app. Both are user/developer-facing brand strings.
Output: 3 files updated. Can execute in parallel with Plans 02 and 04 (different files).
</objective>

<execution_context>
@/Users/qian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-project-rename/11-RESEARCH.md
@.planning/phases/11-project-rename/11-01-SUMMARY.md

**CRITICAL: DO NOT touch `qlji_` in mcp.controller.ts.** Line 713 of `packages/server/src/controller/mcp.controller.ts` contains `"Authorization": "Bearer qlji_YOUR_API_KEY_HERE"` — the `qlji_` prefix here is documentation of the actual API key format users must use. Leave this line unchanged.

**i18n/all.json zh-CN field:** Change both `zh-CN` (line 47, currently "i18n Platform") and `en-US` (line 48, currently "qlj-i18n") to "Transweave" — the brand name is not localized.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MCP service name</name>
  <files>packages/server/src/service/mcp.service.ts</files>
  <action>
Edit `packages/server/src/service/mcp.service.ts` line 24:
Change `name: 'qlj-i18n-mcp-server'` to `name: 'transweave-mcp-server'`

This is the name passed to the `McpServer` constructor. It is what AI coding assistants (Claude, Cursor, Copilot) see when they connect to the MCP endpoint.

Only this one line changes. Do not modify any other lines in the file.
  </action>
  <verify>
    <automated>grep -n "mcp-server\|name:" /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/server/src/service/mcp.service.ts | head -5</automated>
  </verify>
  <done>
    Line 24 reads `name: 'transweave-mcp-server'`.
    No `qlj-i18n-mcp-server` remains in the file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update MCP controller HTML documentation strings</name>
  <files>packages/server/src/controller/mcp.controller.ts</files>
  <action>
Edit `packages/server/src/controller/mcp.controller.ts` with these two changes:

1. Line 591: Change `<span class="info-value">qlj-i18n-mcp-server</span>` to `<span class="info-value">transweave-mcp-server</span>`

2. Line 710: Change `"qlj-i18n": {` (the MCP config key in the HTML example JSON) to `"transweave": {`

**DO NOT change line 713**: `"Authorization": "Bearer qlji_YOUR_API_KEY_HERE"` — the `qlji_` prefix is the actual API key format. Also do NOT change lines 28 and 33 if they contain `qlji_` — those are API key routing signals.

Only change the two lines above (591 and 710). Verify after editing that `qlji_` still appears in the file.
  </action>
  <verify>
    <automated>grep -n "qlj-i18n\|transweave-mcp-server\|qlji_" /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/server/src/controller/mcp.controller.ts | head -20</automated>
  </verify>
  <done>
    Line 591 shows `transweave-mcp-server`.
    Line 710 shows `"transweave": {`.
    `qlji_` still appears in the file (preserved on line 713 and any other lines referencing it).
    Zero remaining `qlj-i18n-mcp-server` or `"qlj-i18n": {` strings.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update web UI i18n header title string</name>
  <files>packages/web/i18n/all.json</files>
  <action>
Edit `packages/web/i18n/all.json` in the `header.title` section:

- Line 47: Change `"zh-CN": "i18n Platform"` to `"zh-CN": "Transweave"` (brand name is not localized)
- Line 48: Change `"en-US": "qlj-i18n"` to `"en-US": "Transweave"`

Both language variants should be "Transweave" — the brand name is the same in all languages.

Note: `packages/web/app/layout.tsx` already says "Transweave" in metadata (completed in Phase 10). This i18n/all.json change handles the runtime-loaded header title that appears in the web UI nav bar or page header.
  </action>
  <verify>
    <automated>grep -n "title\|Transweave\|qlj-i18n\|i18n Platform" /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/web/i18n/all.json | head -10</automated>
  </verify>
  <done>
    Both `zh-CN` and `en-US` values for `header.title` are `"Transweave"`.
    No `"qlj-i18n"` or `"i18n Platform"` remains in the header title section.
  </done>
</task>

</tasks>

<verification>
After all three tasks, confirm:

```bash
# MCP server name
grep -n "mcp-server" \
  /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/server/src/service/mcp.service.ts \
  /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/server/src/controller/mcp.controller.ts

# Preserved qlji_ (must still be present)
grep -c "qlji_" /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/server/src/controller/mcp.controller.ts

# Web i18n header
grep -A3 '"title"' /Users/qian/Projects/Qlj/qlj-fe-i18n/packages/web/i18n/all.json | head -10
```
Expected: mcp-server grep shows only `transweave-mcp-server`; qlji_ count > 0; header title shows Transweave for both locales.
</verification>

<success_criteria>
- MCP server name is `transweave-mcp-server` in mcp.service.ts and mcp.controller.ts HTML docs
- MCP client config example in mcp.controller.ts uses `"transweave"` as the config key
- `qlji_` in mcp.controller.ts is preserved (authorization header documentation)
- Web UI header title is "Transweave" for both `zh-CN` and `en-US` in i18n/all.json
</success_criteria>

<output>
After completion, create `.planning/phases/11-project-rename/11-03-SUMMARY.md` following the summary template.
</output>
